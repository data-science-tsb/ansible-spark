---
  - name: Configure Slave List
      template: 
        src: slaves
        dest: "{{ spark_extract_location }}/conf/slaves"

  - name: Start Master
      shell: "{{ spark_extract_location }}/start-master.sh"

  - name: Start Slaves
            shell: "{{ spark_extract_location }}/start-slaves.sh"